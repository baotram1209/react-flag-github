import React from "react";
import { BrowserRouter, Routes, Routes } from "react-router-dom";

const App = () => {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/" element={}>

        </Route>
      </Routes>
    </BrowserRouter>
  );
};

export default App;
